apiVersion: v1
kind: Service
metadata:
  name: linksnap-frontend-service
spec:
  selector:
    app: linksnap-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
