name: Deploy Status

on:
  push:
    branches: [master]
  workflow_dispatch:

# Required permissions for deployments
permissions:
  contents: read
  deployments: write

jobs:
  deploy:
    name: Register Deployment
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://linksnap.centralindia.cloudapp.azure.com
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deployment Success
        run: echo "✅ Deployed to production at https://linksnap.centralindia.cloudapp.azure.com"

